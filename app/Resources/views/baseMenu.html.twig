<nav class="navbar navbar-inverse" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bsNavBar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('jl_platform_homepage') }}">
                <i class="fa fa-home" aria-hidden="true"></i> Accueil
            </a>
        </div>

        <div class="collapse navbar-collapse" id="bsNavBar">
            <ul class="nav navbar-nav">
                {% if app.request.get('_route') == 'jl_platform_portfolio' %}
                    <li class="active">
                        <a href="{{ path('jl_platform_portfolio') }}"><i class="fa fa-briefcase" aria-hidden="true"></i> Portfolio</a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('jl_platform_portfolio') }}"><i class="fa fa-briefcase" aria-hidden="true"></i> Portfolio</a>
                    </li>
                {% endif %}
                {% if app.request.get('_route') == 'jl_platform_competences' %}
                    <li class="active">
                        <a href="{{ path('jl_platform_competences') }}"><i class="fa fa-book" aria-hidden="true"></i> Competences</a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('jl_platform_competences') }}"><i class="fa fa-book" aria-hidden="true"></i> Competences</a>
                    </li>
                {% endif %}
                {% if app.request.get('_route') == 'jl_platform_contact' or app.request.get('_route') == 'jl_platform_contact_post'%}
                    <li class="active">
                        <a href="{{ path('jl_platform_contact') }}"><i class="fa fa-envelope-o" aria-hidden="true"></i> Contact</a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('jl_platform_contact') }}"><i class="fa fa-envelope-o" aria-hidden="true"></i> Contact</a>
                    </li>
                {% endif %}
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
